{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello ! ✅</h1>

    <button><a href="{{ path('missions_list') }}">Missions list</a></button><br/><br/>

    Your super heroes list :
    <ul>
       {% for user in users %}
           <li><a href="{{ path('users_details', {'id': user.id}) }}"> {{ user.username }}</a></li>
       {% endfor %}
    </ul>

    <button><a href="{{ path('create_user') }}">New super hero</a></button>

</div>
{% endblock %}
