{% extends 'base.html.twig' %}

{% block title %}Hello MissionController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello ! ✅</h1>

    <button><a href="{{ path('users_list') }}">Super heroes list</a></button><br/><br/>

    Your missions list:
    <ul>
        {% for mission in missions %}
            <li><a href="{{ path('mission_details', {'id' : mission.id}) }}">{{ mission.name }}</a></li>
        {% endfor %}
    </ul>

    <button><a href="{{ path('create_mission') }}">New mission</a></button>

</div>
{% endblock %}
